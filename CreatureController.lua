local CreatureController = class("CreatureController")

function CreatureController:ctor(targetMgr, data)
	printDebug("CreatureController")
end

function CreatureController:Update()
	printDebug("CreatureController:Update()")
end

return CreatureController
